[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["npm install -g pnpm@9", "pnpm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node packages/backend/dist/index.js"
